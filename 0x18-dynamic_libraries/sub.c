#include <stdio.h>

/**
 * sub - subscribe two integers
 *
 * @a: first parameter nrequired
 * @b: second parameter required
 *
 * Return: difference between a and b
 */
int sub(int a, int b)
{
	return (a - b);
}
