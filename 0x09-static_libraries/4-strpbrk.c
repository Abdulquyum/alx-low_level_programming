#include "main.h"

/**
 * _strpbrk - locates first occurence of string in accept 
 *
 * @s: parameter for string passed
 * @accept: parameter for where string is searched
 *
 * Return: pointer to byte in s that matches byte in accept.
 *
 */
char *_strpbrk(char *s, char *accept)
{
	return (0);
}
