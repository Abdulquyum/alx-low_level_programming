#include "main.h"

/**
 * _strspn -  calculates lenght of s which contains byte of accept
 *
 * @s: parametr for string of which lenght to be calculated
 * @accept: parameter for byte contained in s
 *
 * Return: number of bytes in s
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
