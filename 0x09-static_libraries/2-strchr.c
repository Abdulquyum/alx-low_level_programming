#include "main.h"

/**
 * _strchr - search for first occurence of char c in string s
 *
 * @s: parameter for string to be searched in
 * @c: parameter for character being searched for
 *
 * Return: pointer to first occurence of char c.
 */
char *_strchr(char *s, char c)
{
	return (0);
}
